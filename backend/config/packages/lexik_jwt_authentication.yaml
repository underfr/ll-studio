lexik_jwt_authentication:
    secret_key: '%env(resolve:JWT_SECRET_KEY)%'
    public_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    pass_phrase: '%env(JWT_PASSPHRASE)%'

    # Durée de vie courte du jeton d'accès : il n'est pas révocable, donc on
    # limite la fenêtre d'exploitation en cas de vol. Le renouvellement passe
    # par /api/token/refresh (cf. gesdinet_jwt_refresh_token.yaml).
    token_ttl: 3600
